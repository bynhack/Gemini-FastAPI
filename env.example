# ===== Gemini-FastAPI Docker Compose 环境变量示例 =====

# 认证（可选，不设置则关闭鉴权）
API_KEY=your-secure-api-key

# Gemini 账号 Cookie（至少提供一组）
GEMINI_CLIENT_ID=client-a
SECURE_1PSID=
SECURE_1PSIDTS=

# 日志与请求上限（可选）
LOG_LEVEL=INFO
MAX_CHARS_PER_REQUEST=1000000

# 代理与超时（可选；若无代理请留空）
http_proxy=
https_proxy=
no_proxy=localhost,127.0.0.1,::1
UV_HTTP_TIMEOUT=120

# 端口映射（如需修改请直接改 docker-compose.yml 的 ports 字段）
# 例："8004:8000" 表示宿主 8004 -> 容器 8000
